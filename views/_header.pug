header.header 
    nav.nav.nav--tours 
        a.nav__el(href='/') Все туры
    .header_logo
        img(src='/img/logo-white.png', alt='Natours logo')
    nav.nav.nav--user 
        if user
            a.nav__el.nav__el--logout Выйти
            a.nav__el(href="/me")
                img.nav__user-img(src=`/img/users/${user.photo}`, alt=`Фото ${user.name}`)
                span= user.name.split(' ')[0]
        else
            a.nav__el(href="/login") Войти
            a.nav__el.nav__el--cta(href="#") Присоединится